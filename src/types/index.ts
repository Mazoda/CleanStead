export type ServiceType = {
  imageUrl: string;
  title: string;
  description: string;
  serviceID: number;
};

export type ReasonType = {
  id?: number;
  title: string;
  description: string;
  iconUrl: string;
};
