export type ServiceType = {
  imageUrl: string;
  title: string;
  description: string;
  id: number;
};

export type ReasonType = {
  id?: number;
  title: string;
  description: string;
  iconUrl: string;
};
